name: macOS build

# Builds Tenjin for macOS on macos-14 and publishes an unsigned .dmg as a
# Release asset. Same optimizations as the Linux pipeline:
#
#   - install-qt-action with cache → warm Qt restore in ~5s.
#   - ccache caches object files across runs (clang + ccache integrates
#     cleanly via the CMake LAUNCHER variables, same wiring as Linux).
#   - Ninja for parallel builds.
#   - Tests / benchmarks / sanitizers off → only ship code is built.
#   - Concurrency group cancels in-flight runs on the same branch.
#
# Signing is intentionally not done in CI — adding an Apple Developer ID
# requires a paid Apple account and a secret keychain in the runner. Run
# `xcrun codesign --deep --force --sign "<your-identity>" Tenjin.app` locally
# and re-zip into a .dmg if you need a signed build.

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: macos-${{ github.ref }}
  cancel-in-progress: true

jobs:
  macos-build:
    runs-on: macos-14
    permissions:
      contents: write

    env:
      TENJIN_APP_NAME: Tenjin
      CCACHE_MAXSIZE: 500M
      CCACHE_COMPRESS: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Load .env into environment
        run: |
          echo "TENJIN_APP_NAME=Tenjin" >> "$GITHUB_ENV"
          if [ -f .env ]; then
            awk -F= '
              /^[[:space:]]*#/ { next } /^[[:space:]]*$/ { next } NF < 2 { next }
              { k=$1; $1=""; v=substr($0,2);
                sub(/^[[:space:]]+/,"",k); sub(/[[:space:]]+$/,"",k);
                sub(/^[[:space:]]+/,"",v); sub(/[[:space:]]+$/,"",v);
                if (v ~ /^".*"$/ || v ~ /^'\''.*'\''$/) v=substr(v,2,length(v)-2);
                print k "=" v
              }
            ' .env >> "$GITHUB_ENV"
          fi

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Install Homebrew tools
        run: |
          brew install ninja ccache create-dmg

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.8.3"
          host: "mac"
          target: "desktop"
          arch: "clang_64"
          modules: "qtmultimedia"
          dir: ${{ github.workspace }}/Qt
          cache: true

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/ccache
          key: macos-ccache-${{ github.sha }}
          restore-keys: |
            macos-ccache-

      - name: Configure (CMake, Ninja)
        run: |
          QT_MAC="${{ github.workspace }}/Qt/Qt/6.8.3/macos"
          export PATH="$QT_MAC/bin:$PATH"
          cmake -G Ninja -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_PREFIX_PATH="$QT_MAC" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="12.0" \
            -DMEDIA_SUPPORT=OFF \
            -DWEBVIEW_SUPPORT=OFF \
            -DBUILD_TESTS=OFF \
            -DBUILD_BENCHMARKS=OFF \
            -DSANITIZERS="" \
            -DTENJIN_BUILD_NUMBER="${{ github.run_number }}"

      - name: Build
        run: |
          ccache --max-size=$CCACHE_MAXSIZE
          ccache --zero-stats
          cmake --build build --config Release
          echo "===== ccache stats ====="
          ccache --show-stats

      - name: Locate .app bundle
        run: |
          APP=$(find build -name "$TENJIN_APP_NAME.app" -type d | head -n1)
          if [ -z "$APP" ]; then
            echo "::error::$TENJIN_APP_NAME.app not found in build/"
            find build -maxdepth 5 -name '*.app' -type d
            exit 1
          fi
          echo "TENJIN_APP_BUNDLE=$APP" >> "$GITHUB_ENV"
          echo "Bundle: $APP"

      # macdeployqt bundles Qt frameworks/plugins into the .app so it can
      # run on any macOS 12+ machine without a system Qt install.
      - name: macdeployqt
        run: |
          QT_MAC="${{ github.workspace }}/Qt/Qt/6.8.3/macos"
          "$QT_MAC/bin/macdeployqt" "$TENJIN_APP_BUNDLE" -verbose=1 -always-overwrite

      # Ad-hoc sign so dyld accepts the bundle on the user's machine without
      # them having to drop quarantine attributes manually.
      - name: Ad-hoc codesign
        run: |
          codesign --force --deep --sign - --timestamp=none "$TENJIN_APP_BUNDLE"
          codesign -dv --verbose=2 "$TENJIN_APP_BUNDLE" 2>&1 | head -20

      - name: Package .dmg
        run: |
          DMG="${TENJIN_APP_NAME}-${{ github.run_number }}.dmg"
          rm -f "$DMG"
          create-dmg \
            --volname "$TENJIN_APP_NAME" \
            --window-pos 200 120 \
            --window-size 600 360 \
            --icon-size 100 \
            --icon "$(basename $TENJIN_APP_BUNDLE)" 175 180 \
            --hide-extension "$(basename $TENJIN_APP_BUNDLE)" \
            --app-drop-link 425 180 \
            "$DMG" \
            "$(dirname $TENJIN_APP_BUNDLE)" || true
          # create-dmg occasionally exits non-zero on the cleanup step even
          # after successfully producing the .dmg. Verify the file exists.
          if [ ! -f "$DMG" ]; then
            echo "::error::.dmg not produced"
            exit 1
          fi
          echo "TENJIN_DMG=$DMG" >> "$GITHUB_ENV"
          du -h "$DMG"

      - name: Publish .dmg to a GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: macos-build-${{ github.run_number }}
          name: macOS build ${{ github.run_number }}
          files: ${{ env.TENJIN_DMG }}
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
